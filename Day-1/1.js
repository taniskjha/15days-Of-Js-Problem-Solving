// Array of Word Lengths

// 1 -length of array strings
// output = array of string lengths


function wordLengths(arr) {

    return arr.map(element => element.length)
       
   }
   
   arr(["hello", "world"]);